export interface TextElement {
  id: string;
  text: string;
  fontSize: number;
  x: number;
  y: number;
  fontFamily?: string;
  color?: string;
}

export interface ImageDimensions {
    width: number;
    height: number;
  }